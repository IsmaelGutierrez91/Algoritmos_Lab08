using Sirenix.OdinInspector;
using System.Collections;
using System.Collections.Generic;
using Unity.VisualScripting;
using UnityEngine;

public class GameManager : MonoBehaviour
{
    /*
    public GameObject LVL1;
    public GameObject LVL2;
    public GameObject LVL3;
    public Graph<int, GameObject> Grafos = new Graph<int, GameObject>();
    public List<GameObject> LetterNodes = new List<GameObject>();
    public int currentNodes = 0;

    private void Start()
    {
        AddNewNode(LVL1);
        AddNewNode(LVL2);
        AddNewNode(LVL3);
    }
    [Button("Add a new node")]
    public void AddNewNode(GameObject letterNode)
    {
        Grafos.AddNode(currentNodes, letterNode);
        currentNodes = currentNodes + 1;
        LetterNodes.Add(letterNode);
        Debug.Log("New node is added");
    }
    [Button("Connect two nodes")]
    public void ConnectNodes(int node1ID, int node2ID)
    {
        Grafos.AddEdge(node1ID, node2ID);
        //LetterNodes[node2ID].GetComponent<LineDrawer>().SetTarget(LetterNodes[node1ID].transform);
        Debug.Log("The conection is maked");
    }
    [Button("Show Matrix")]
    public void ShowMatrix()
    {
        Grafos.DisplayGraphAsMatrix();
    }
    */
}
